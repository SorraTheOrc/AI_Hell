import './style.css';
import { Game } from './core/Game';

// Browser entry point: boot the AI_Hell game (creates the Phaser canvas
// inside #game-container). This is the web distribution model from GDD §6.3.
new Game();