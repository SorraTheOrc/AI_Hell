{"__worklog_sync__":{"kind":"delta","version":1}}
{"data":{"assignee":"Map","createdAt":"2026-08-27T22:07:05.955Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"## Updated (2026-09-02): Operator-requested sizing change — POWER_UP_LIFETIME increased from 5 s to 12.5 s (2.5×); POWER_UP_DROP_SIZE reduced from 32 px to 8 px (75% smaller). Spawn cadence adjusted to 12.5 s to maintain single-drop cycle. All tests updated and green (602 total). All audit gaps closed: combat effects/icon tests added, shield bubble/phase ghost visuals implemented, P4 bomb notice, HUD lives counter hidden via options. All 12 children in_review.\n\nOriginal description follows:\n\n# Create combat gym scene for combat-coupled power-ups (P3 Shield, P4 Bomb, P6 Phase Shift, P7 Teleport) with low-level enemy threats","effort":"Small","id":"AH-0MTC2P6G3007PJ40","issueType":"feature","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"High","sortIndex":200,"stage":"in_review","status":"completed","tags":[],"title":"Create combat gym scene for combat-coupled power-ups (P3 Shield, P4 Bomb, P6 Phase Shift, P7 Teleport) with low-level enemy threats","updatedAt":"2026-09-09T21:54:09.012Z"},"type":"workitem"}
{"data":{"assignee":"Map","createdAt":"2026-08-30T10:48:59.387Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Player thruster SFX — Intake Brief\n\n## Problem Statement\n\nThe player ship has no thruster audio feedback. Thruster sound effects should rise continuously from silence to a sustained hum whose volume (and optionally pitch) is proportional to the ship's configured thrust acceleration value, so tuning the `thrustAcceleration` slider (GDD §2.2, `ShipConfig`) is legible by ear as well as by the animated flame.\n\n## Users\n\n- Player piloting the ship in any gym scene (either control scheme), relying on thruster hum to confirm thrust input and tuning changes without watching the flame.\n- Developer tuning ship physics via the config sliders, hearing the acceleration change immediately.\n\n## User Stories\n\n- As a player holding a thrust key, I hear a thruster hum that fades in from silence so the onset of thrust is not a click/pop.\n- As a player holding thrust at low `thrustAcceleration`, I hear a quieter/lower hum than at high `thrustAcceleration`, so the slider is audible.\n- As a player releasing all thrust keys, I hear the hum decay quickly to silence, matching the flame shrink-back.\n- As a player switching to the Asteroids scheme, forward thrust and turn thrust (side engines) produce the same thruster hum via the shared `getEngineSoundLevel` interface (VFX-matching key-specific behaviour stays visual-only; audio is a single ship-level hum).\n\n## Acceptance Criteria\n\n- **AC1 — Procedural thruster hum function(s):** `src/audio/effects.ts` exposes a thruster SFX interface (e.g. `startThrusterSound`/`stopThrusterSound`/`setThrusterLevel` or a single `updateThrusterSound(level: number)` that internally manages a sustained oscillator+gain) using Web Audio synthesis (no external assets). Distinct from all existing blip/cue sounds. Degrades to safe no-op without an AudioContext (headless tests, autoplay-blocked browsers).\n- **AC2 — Level scales with acceleration value:** The audible level is 0 when not thrusting and rises to a configured maximum when thrusting, scaled by `ShipConfig.thrustAcceleration` (reference `FLAME_REF_THRUST = 300` / `DEFAULT_CONFIG.thrustAcceleration`). At `thrustAcceleration = 0` the hum stays silent (consistent with `flameGrowthRate` returning 0). Higher thrust reaches full volume faster and reads louder/higher — at minimum the gain is proportional to `thrustAcceleration / FLAME_REF_THRUST` (clamped to [0, 1]).\n- **AC3 — Rise from nothing (fade-in) and quick decay:** When thrust begins, gain ramps from 0 to the target level over a short window (≤ 100 ms at reference thrust, faster at higher thrust — analogous to `FLAME_GROWTH_TIME_AT_REF = 30 ms`). When thrust stops, gain decays to 0 at ~4× the growth rate (mirroring `FLAME_SHRINK_MULTIPLIER = 4`), with no clicks/pops (gain envelope). Retriggering thrust while still decaying restarts the ramp from the current gain.\n- **AC4 — Scheme-agnostic wiring via movement model:** `MovementModel.getEngineSoundLevel` returns a continuous level in [0, 1] for the SFX layer to consume (FourDirectional: 0 when no direction held, 1 when any thrust key held, scaled by acceleration; Asteroids: 0 when idle, 1 when forward/turnLeft/turnRight held, scaled similarly). Player or scene code drives the audio from the model's level each frame (e.g. `Player.preUpdate` or the active scene's update), without affecting physics (`tick` unchanged).\n- **AC5 — No overlapping/orphaned audio:** Holding thrust does not stack multiple oscillators; releasing and re-pressing does not leak AudioNodes. Rapid toggling is clean. If the ship is destroyed/respawned or the scene switches, the hum stops.\n- **AC6 — Testing/validation:** Unit tests verify (a) the thruster audio node respects AudioContext absence (no-op), (b) `getEngineSoundLevel` returns 0 at idle and > 0 / scaled proportionally when thrusting (including thrust=0 edge case), (c) gain/volume ramping exists (e.g. gain envelope calls or oscillator lifecycle), and (d) scene/Player wiring drives the hum per frame while thrusting and silences it when idle. Full project test suite passes with no regressions.\n- **AC7 — Documentation:** GDD §7.3 Audio Direction — Player Audio Character table updated with the thruster hum row (synthesis, wave, contour, volume, thrust-scaling note). Code comments document the thruster interface, thrust-scaling rationale, and autoplay/no-AudioContext fallback.\n- **AC8 — Full project test suite must pass with the new changes.**\n\n## Constraints\n\n- Procedural Web Audio only — no external audio assets (GDD §7.3).\n- Safe no-op without AudioContext; respects browser autoplay policy (silent until user gesture).\n- Must not introduce physics changes — `MovementModel.tick` and flame animation (`src/utils/flame.ts`) remain behaviourally unchanged; only `getEngineSoundLevel` and the audio integration change.\n- Volume ceiling ≤ 0.2 to stay consistent with existing player cues (GDD §7.3 \"All player cues keep volume ≤ 0.2\").\n- Backward compatible with both control schemes; does not regress existing VFX (per-engine flame) or shoot/pickup audio.\n\n## Existing State\n\n- `src/utils/movementModel.ts`: `getEngineSoundLevel` is a binary placeholder (returns 0 or 1) for both models; `getEngineActivity` is key-specific (AH-0MTFORPJ2003RWWQ). `flame.ts` already models growth ∝ `thrustAcceleration` with 30 ms to full at ref thrust and 4× decay.\n- `src/audio/effects.ts`: 11+ procedural cues (enemy spawn/destruction, Tank/Scout/Diver, Swarm, player shoot/pickups) all via `blip()` or discrete oscillators; no continuous/looping hum and no thruster audio yet.\n- `src/entities/Player.ts`: `preUpdate` animates per-engine flame lengths via `getEngineActivity` + `flame.ts`; no audio calls for thrusters. `ShipConfig.thrustAcceleration` defaults to 300 and is live-tunable via gym sliders.\n- Docs: GDD §7.3 catalogs player shoot/pickup audio but has no thruster hum entry; §2.2 describes the flame scaling with `thrustAcceleration`.\n\n## Desired Change\n\n- `src/audio/effects.ts`: add a sustained thruster hum (e.g. low sawtooth/square + sine undertone, filtered hum) with an API that a per-frame caller can drive: create/reuse a single oscillator+gain pair, ramp gain from 0 to `level * maxVolume` (level ∈ [0, 1] from the movement model), and stop/cleanup on silence. Ensure exponential/linear gain ramps avoid clicks and the node lifecycle does not leak.\n- `src/utils/movementModel.ts`: upgrade `getEngineSoundLevel` from binary to a continuous level scaled by `thrustAcceleration` (e.g. `level = (forward||turn||anyDir ? min(1, thrustAcceleration/FLAME_REF_THRUST) : 0)` or a dedicated param) — or document that the caller scales the binary level by the config value; keep `getEngineActivity` unchanged.\n- `src/entities/Player.ts` (or active gym scenes): in `preUpdate`/scene update, derive the current thruster level from `getEngineSoundLevel` + config and call the audio interface each frame; ensure the hum follows the pressed keys (4-dir: any arrow/WASD; Asteroids: W/Up, A/Left, D/Right) and stops when the model reports 0.\n- Tests: `src/utils/movementModel.test.ts`, `src/audio/effects.test.ts`, and Player/scene tests updated to assert scaling, no-op, and wiring.\n- Docs: GDD §7.3 and inline comments updated.\n\n## Key Files\n\n- `src/audio/effects.ts` — Needs sustained thruster hum with thrust-scaled gain/pitch and safe no-op\n- `src/utils/movementModel.ts` — Needs `getEngineSoundLevel` upgraded to continuous [0,1] scaled by thrustAcceleration\n- `src/entities/Player.ts` — Needs per-frame audio driving from model level (preUpdate integration); verify no physics change\n- `src/utils/flame.ts` — Reference for thrust-proportional growth/decay model (reuse timing constants analogously)\n- `src/core/config.ts` — ShipConfig.thrustAcceleration source of scaling; no change expected (read-only)\n- `src/utils/movementModel.test.ts` — Needs thruster-level unit tests (binary→scaled, thrust=0 edge)\n- `src/audio/effects.test.ts` — Needs thruster hum oscillator/gain + no-op tests\n- `docs/Game Design Document.md` — §7.3 Player Audio Character — Needs thruster hum row + scaling note\n\n## Related Work\n\n- **AH-0MTFORPJ2003RWWQ** — Asteroids directional VFX improvements (key-specific flame selection; `getEngineSoundLevel` placeholder — direct interface to extend)\n- **AH-0MTGBPRYC000QIHU** — Player has no audio (shoot + pickup procedural cues — established `effects.ts` Web Audio patterns and GDD §7.3 Player Audio Character table)\n- **AH-0MTF0EFNZ000RPVD** — Alternative player control (pluggable movement models, 3-engine layout, `getEngineSoundLevel` origin Q4)\n- **AH-0MT97BW7M0056S8S** — Gym scene with player ship and thrust-based movement (flame VFX, `ShipConfig`)\n- **AH-0MTAF5YLT009TXAM** — Thrust flame scales with `thrustAcceleration` (flame.ts growth model — direct scaling precedent)\n- **AH-0MTGS4P0V005MTSN** — Add 4 player shoot audio cues (wave-type distinctiveness precedent — thruster hum must stay distinct)\n\n## Risks & Assumptions\n\n- **Scaling basis assumption:** \"acceleration value\" is interpreted as `ShipConfig.thrustAcceleration` (not velocity or instantaneous force). Gain/pitch thus scales with the config slider; idle = silent, max ≈ 0.15–0.2. If the producer meant a different acceleration metric, the scaling factor can be swapped with minimal code change.\n- **Single hum vs per-engine hums:** Assumed single ship-level hum (not one oscillator per engine port). Per-engine layering would complicate mixing and is out of scope; can be added as a follow-up if side-thruster distinctiveness is desired.\n- **Continuous vs discrete:** Assumed sustained oscillator+gain (not repeated blips) to meet \"rise from nothing\" — requires careful AudioNode lifecycle to avoid leaks. Mitigation: single reused node with gain ramping + unit tests for cleanup.\n- **Autoplay/silent-until-gesture:** Hum will be silent until the first user interaction (browser policy) — same as all other SFX; documented in GDD §7.3.\n- **Scope-creep risk:** Per-engine spatialisation, pitch-bend on velocity, or background music integration are out of scope — track as `discovered-from:AH-0MTFOSOHN001Q620` work items.\n\n## Appendix: Clarifying Questions\n\n- Q1 — Scaling basis: \"rise from nothing to thruster sound based on the acceleration value\" — should the audible level scale with (a) `ShipConfig.thrustAcceleration` (the tuning slider, analogous to flame growth), (b) instantaneous thrust input (binary held/released), (c) current ship velocity, or (d) a combination? Assumed (a) scaled by held-state (binary × config ratio) — confirm or correct. — Answer: *(open — assumed (a); awaiting producer confirmation)* Source: intake assumption; reference `src/core/config.ts:DEFAULT_CONFIG.thrustAcceleration`, `src/utils/flame.ts:flameGrowthRate`.\n- Q2 — Hum character: Should the thruster be a continuous looping hum/drone (sustained oscillator with gain ramp) or a one-shot sweep per thrust press? Should it differ between forward vs turn thrust? Assumed continuous single hum with gain ramp, not per-engine distinct tones — confirm. — Answer: *(open — assumed continuous single hum)* Source: intake assumption; reference GDD §7.3 procedural approach, `src/audio/effects.ts` existing continuous cues (Tank whine).\n- Q3 — Integration scope: Which scenes should hear the hum — every scene that hosts the player (all gyms + future combat scenes) or a single gym first? Assumed Player-driven in `Player.preUpdate` so every host scene inherits it — confirm scope and whether a mute/volume setting should apply. — Answer: *(open — assumed Player-preUpdate, all host scenes,≤ 0.2 volume)* Source: intake assumption; reference `src/entities/Player.ts:preUpdate`, `src/scenes/gym/*`.\n\n","effort":"Small","id":"AH-0MTFOSOHN001Q620","issueType":"feature","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"Medium","sortIndex":200,"stage":"in_review","status":"completed","tags":[],"title":"Player thruster SFX","updatedAt":"2026-09-09T21:54:03.246Z"},"type":"workitem"}
{"data":{"assignee":"Map","createdAt":"2026-08-30T14:54:24.110Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# When all enemies in enemy gym scene have been killed respawn them (AH-0MTFXKA5Q003LBH5)\n\n## Problem statement\n\nEnemy gym scenes (`GymScout`, `GymDiver`, `GymTank`, `GymPhaser`, `GymSwarm`) allow the player to destroy every enemy (1 HP each, `alive === false` after `destroySelf()`), leaving an empty field with nothing to do. When the formation is fully cleared, start a visible 3-second countdown and then respawn the full formation so the gym remains usable without reloading the scene.\n\n## Users\n\n- **Player / tester exercising an enemy gym** — wants continuous targets to practice flight, aiming, and weapon feel without manually refreshing the scene.\n- **Developer / AI agent verifying enemy behaviour** — wants a deterministic, observable wipe → countdown → respawn loop that can be asserted in automated tests.\n\n### Example user stories\n\n- As a **player in any enemy gym**, when I destroy the last alive enemy, I see a 3-second countdown (e.g. centred `Respawning in 3…2…1…`) and then a fresh formation appears at its starting position so I can keep shooting.\n- As a **developer**, I want the wipe detection and respawn to live in the shared core (`GymFormationScene`) so all five formation gyms inherit it without per-scene duplication, and the behaviour is covered by scene tests.\n\n## Acceptance Criteria\n\n- [ ] **AC1 — Wipe detection and 3s countdown:** When `aliveCount` transitions to `0` (all `FormationSceneEntity.alive === false`), the scene starts a 3-second countdown and displays a visible countdown indicator (text overlay). The countdown ticks once per second and is observable for tests (getter or text contents).\n- [ ] **AC2 — Automatic respawn after countdown:** After exactly 3 seconds (within tick tolerance), the full formation respawns: `aliveCount` returns to `config.count`, entities reappear at `config.startX/startY`-derived positions, formation base resets to its start, and the countdown indicator is removed. `playSpawnSound()` (or equivalent) is triggered on respawn. Enemy bullets in flight are cleared so a stale bullet does not instantly hit the player.\n- [ ] **AC3 — Applies to all formation gyms via the core library:** `GymScout`, `GymDiver`, `GymTank`, `GymPhaser`, `GymSwarm` all exhibit AC1–AC2 with no per-scene duplication beyond config. The change is implemented in `src/scenes/gym/core/GymFormationScene.ts` (new fields/methods optional and backward compatible). `GymBoss` (multi-phase boss, separate scene) is out of scope unless explicitly noted.\n- [ ] **AC4 — Combat and chrome preserved:** Player ship (arrows + WASD), `EXPLODE` / `SHOOT: ON/OFF` buttons, status/hint lines, drift/respawn off the right edge, and existing collision/explosion VFX/SFX behaviour remain unchanged except for the wipe→respawn addition. `shootEnabled` state carries over across the respawn (no surprise toggle).\n- [ ] **AC5 — Testing and full suite:** Automated tests cover wipe detection, countdown progression (including that the countdown restarts correctly if the scene is torn down), successful respawn with correct positions/count, bullet cleanup on respawn, and that the existing `GymFormationScene` / per-gym tests still pass. Full project test suite passes (`npm test`).\n- [ ] **AC6 — Documentation:** `docs/ENEMY_DESIGN_AND_IMPLEMENTATION.md` updated to document the wipe → 3s countdown → respawn convention (including that it is core-library owned); related comments updated.\n\n## Constraints\n\n- No game-over, lives, or scoring changes — gyms remain demo/testbeds (infinite lives, as with player hit/respawn at `PLAYER_RESPAWN_INVULNERABLE = 1.5s`).\n- No collision engine — continue manual distance checks for any new collision-adjacent work.\n- Prefer core-library extension over per-scene boilerplate; keep `EnemyFormationConfig` backward compatible.\n- Use existing sounds (`src/audio/effects.ts`) and existing entity `destroySelf()`/`alive` contract; do not introduce a new entity type.\n\n## Existing state\n\n- Five enemy gyms are thin `GymFormationScene<TEntity,TBullet>` subclasses driven by `EnemyFormationConfig` (`src/scenes/gym/core/GymFormationScene.ts`): formation spawn, HUD buttons, status/hint/`← INDEX`, drift + `_respawnX()` off the right edge, per-entity `applyFormationPosition()`, fire-bullet collection + advance + cull, player spawn/input/auto-fire/bullet lifecycle/collisions/invulnerability blink.\n- Enemies are 1 HP; destroying the last one leaves `aliveCount === 0` with no recovery — the scene stays empty indefinitely.\n- Existing tests live in `src/scenes/gym/core/GymFormationScene.test.ts` and `src/scenes/gym/Gym*.test.ts`; player hit/respawn is already covered (explosion VFX, `respawn()`, blink).\n\n## Desired change\n\n- Extend `GymFormationScene` to detect `aliveCount === 0`, start and tick a 3-second countdown (visible text, deterministic via `tick(dt)`), then on expiry clear enemy bullets, destroy/recreate or resurrect the formation at its initial geometry, reset `formationBaseX/Y` to `startX/Y`, refresh `statusText`, hide the countdown, and play the spawn sound. Expose minimal test seams (e.g. `isRespawnCountdownActive()`, `getRespawnCountdownRemaining()` or equivalent).\n- Update `docs/ENEMY_DESIGN_AND_IMPLEMENTATION.md` §2–§3 to note the wipe/respawn lifecycle.\n\n## Key Files\n\n- `src/scenes/gym/core/GymFormationScene.ts` — Add wipe detection, 3s countdown state/tick, countdown text overlay, bullet cleanup, and formation respawn logic; expose test getters.\n- `src/scenes/gym/core/GymFormationScene.test.ts` — Add AC1–AC3 tests for the core library.\n- `src/scenes/gym/GymScout.ts` (and `GymDiver.ts`, `GymTank.ts`, `GymPhaser.ts`, `GymSwarm.ts`) — No code change expected, but verify via per-scene smoke/regression tests that the core behaviour is inherited.\n- `docs/ENEMY_DESIGN_AND_IMPLEMENTATION.md` — Document the wipe → countdown → respawn convention.\n- `src/audio/effects.ts` — Reuse `playSpawnSound()` on respawn (no change unless a distinct sound is desired).\n\n## Related work\n\n- **AH-0MTD0Z0Y7003T1NT** — Add player to each Enemy Gym scene (completed, parent of the combat interactions `destroySelf()`/`alive` that define \"killed\").\n- **AH-0MTECNG8W00403EH / AH-0MTECNG4K0071TIB** — Core scene: collision detection and player hit/respawn (establishes `aliveCount`, manual collisions, `Player.respawn()` pattern to reuse).\n- **AH-0MTAN4IFR007U9B3 / AH-0MT985FOA008JWM9 / AH-0MTAN4T0I004H187** — Core-library extraction and rewrites that make this a one-place change.\n- **AH-0MT99* family** — `Create E1–E5 Enemy Gym Scene` items (consumers of the core contract).\n\n## Risks & assumptions\n\n- **Assumption:** \"Killed\" means `alive === false` after `destroySelf()` (including explosion VFX still playing) — i.e. `aliveCount === 0` is the wipe signal, not graphics lifecycle. Mitigation: state this explicitly and assert on `aliveCount`.\n- **Assumption:** 3s countdown is wall-clock seconds driven by `tick(dt)` / `time.now`, not frames, and is **visible** to the player (centred countdown text). If the producer prefers a silent timer or a different presentation, adjust the overlay without changing the timing contract. Recorded as open clarification below.\n- **Assumption:** Respawn resets the formation to `startX/startY` with the original `count/spacing` geometry and clears enemy bullets only (player bullets persist). If producer wants player bullets cleared or score/status preservation, note as follow-up.\n- **Risk:** Countdown races with the formation drift `_respawnX()` (right-edge respawn) — they are orthogonal. Mitigation: wipe/respawn resets `formationBaseX/Y` and does not interfere with the drift timer.\n- **Risk:** Scope creep — adding scoring waves, lives, or boss waves. Mitigation: this item is strictly wipe → 3s → same formation; any wave/score work goes to a linked follow-up.\n\n## Appendix: Clarifying questions & answers\n\n- Q: \"Should the 3-second countdown be visible to the player, and if so where/how (e.g. centred text `Respawning in 3…`, status line countdown, both)?\" — Answer: **Assumed visible centred countdown text** (e.g. `Respawning in 3…` ticking to `1…`) plus status line update; change presentation if producer prefers differently. Source: researcher inference from seed text (\"3 second countdown\") — no prior worklog entry specifies presentation; left as producer-reviewable assumption.\n- Q: \"On respawn, should the formation reset to `startX/startY` (as on initial `create()`) or continue from its current drift position? Should `shootEnabled` and any per-entity state be preserved or reset?\" — Answer: **Assumed full reset to start position with `shootEnabled` preserved**, fresh entities (or resurrected) at initial offsets. Source: prior gym convention (spawn at `startX/startY`) and minimal-surprise principle; flagged for producer confirmation.\n- Q: \"Should enemy bullets (and/or player bullets) be cleared when the countdown ends and the formation respawns?\" — Answer: **Assumed enemy bullets cleared, player bullets kept**. Source: prevents a stale enemy bullet from hitting the freshly respawned player; avoids surprising the player by deleting their shots. Flagged for producer review.\n- Q: \"Does this apply to `GymBoss` or only the five formation gyms? Does 'killed' include mid-explosion (`alive === false` but explosion animating) or only after animation completes?\" — Answer: **Assumed formation gyms only (`GymScout`/`GymDiver`/`GymTank`/`GymPhaser`/`GymSwarm`); 'killed' = `aliveCount === 0` (mid-explosion counts)**. Source: Boss is multi-phase and tracked separately (`AH-0MT99QBDW001O7PE` family); `alive` contract from `GymFormationScene`.\n\n","effort":"medium","id":"AH-0MTFXKA5Q003LBH5","issueType":"feature","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"medium","sortIndex":100,"stage":"in_review","status":"completed","tags":[],"title":"When all enemies in enemy gym scene have been killed respawn them","updatedAt":"2026-09-09T21:53:40.135Z"},"type":"workitem"}
{"data":{"assignee":"agent","createdAt":"2026-08-30T18:40:02.855Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Power ups need to be larger — Intake Brief (AH-0MTG5MGPZ00986B4)\n\n## Problem statement\n\nPower-up drops are too small and visually inconspicuous, making them difficult to pick up during play. Players need drops to be twice as large with a clearly visible glowing bubble (circle) around each drop so pickups are immediately legible and more forgiving to collect.\n\n## Users\n\n- **Players** — struggle to collect small drops under fire; benefit from larger, high-contrast pickups that are easier to see and overlap.\n- **Gym testers / developers** — need the sizing and bubble semantics defined once so every drop type renders consistently.\n\n### Example user stories\n\n- As a **player**, I want power-up drops to be twice as large so I can collect them without pixel-perfect positioning.\n- As a **player**, I want each drop surrounded by a glowing bubble/circle so I can spot it instantly against the black background and busy bullet patterns.\n- As a **developer**, I want the size and bubble defined as tunable constants / shared visuals so GymPowerUps and GymWeapons both render the same way.\n\n## Acceptance Criteria\n\n- [ ] **AC1 — Drops are twice as large:** `POWER_UP_DROP_SIZE` and `WEAPON_DROP_SIZE` (canonical in `src/core/constants.ts`) are doubled from 16 → 32 px (or equivalent scale factor of 2×). All drop types use the new size, including non-combat (P5, P8, P9) and weapon (Spread, Dual, Rapid, Reset). Mirrored/re-exported constants (`src/powerups/PowerUp.ts`, gym scenes) remain consistent.\n- [ ] **AC2 — Glowing bubble around every drop:** Each on-field drop renders a glowing circle/bubble surrounding its icon. The bubble is code-drawn (Phaser Graphics, no external assets), in the neon style (e.g., per-type aura colour or white with outer glow), centred at the drop position and scaled with the lifecycle scale (grow/hold/shrink). GymPowerUps renders the bubble (currently no drop graphics — must add) and GymWeapons adds the bubble alongside its existing weapon icon graphics.\n- [ ] **AC3 — Collection remains correct and more forgiving:** Overlap collection (`_overlapsShip` in both gym scenes) uses the new size (`DROP_SIZE * currentScale + hull`) so the effective pickup radius is doubled at full scale. Drops are still gated by the >3% collection threshold; collecting plays the existing per-type audio.\n- [ ] **AC4 — Lifecycle visuals scale correctly:** Bubble + icon scale 0 → 1 → 0 with the `PowerUp` lifecycle (`advance()`) so the growing/shrinking animation remains smooth and framerate-independent; fully despawned drops remove their graphics.\n- [ ] **AC5 — Testing/validation:** Existing drop/collection/magnet/weapon suites still pass with the new size (overlap thresholds updated where they hard-code 16). At least one new or updated test asserts: (a) the doubled size constant, (b) the bubble graphics are created per drop and scaled with lifecycle, (c) overlap distance at full scale is doubled. Manual visual check in GymPowerUps and GymWeapons shows the bubble clearly.\n- [ ] **AC6 — All related documentation is updated to reflect the changes, including code comments, README, and any relevant wiki or docs site entries.**\n- [ ] **AC7 — Full project test suite must pass with the new changes.**\n\n## Constraints\n\n- Code-first, no external art assets — bubble is drawn with Phaser Graphics / lineStyle primitives (neon vector aesthetic, GDD §7.1).\n- Engine: Phaser v4 + TypeScript; web + Windows binary compatible (GDD §6.3).\n- Tunable size lives in `src/core/constants.ts` (single source of truth); scenes and `src/powerups/` re-export or import from there.\n- Do not change effect semantics, lifetimes, spawn cadence, or magnet math — only size and visuals.\n\n## Existing state\n\n- `src/core/constants.ts` defines `POWER_UP_DROP_SIZE = 16` and `WEAPON_DROP_SIZE = POWER_UP_DROP_SIZE`; `HUD.ts` draws HUD icons at size 8 (not in scope).\n- `src/powerups/icons.ts` draws per-type power-up icons (`drawPowerUpIcon`) and weapon icons (`drawWeaponIcon`) at the caller-supplied size; no bubble is drawn today.\n- `src/scenes/gym/GymPowerUps.ts` tracks `ActiveDrop { powerUp, x, y }` with no per-drop Graphics — drops have no on-screen visuals beyond lifecycle bookkeeping. Overlap uses `POWER_UP_DROP_SIZE * currentScale + hull`.\n- `src/scenes/gym/GymWeapons.ts` creates a `Graphics` per weapon drop, draws the weapon icon at `WEAPON_DROP_SIZE`, and scales it with `currentScale`; no bubble.\n- `src/powerups/PowerUp.ts` / `effects.ts` / `spawner.ts` are size-agnostic (lifecycle is time-based, not px-based).\n\n## Desired change\n\n1. Double the canonical drop size constant (16 → 32) in `src/core/constants.ts`; ensure `WEAPON_DROP_SIZE` follows and re-exports stay consistent.\n2. Add a glowing bubble visual for every on-field drop:\n   - Introduce or extend a shared helper (e.g., in `src/powerups/icons.ts` or a small visuals helper) that draws a neon circle/bubble around the icon.\n   - GymPowerUps: add per-drop Graphics (icon + bubble) with lifecycle-scaled `setScale(currentScale)` and proper `destroy()` on despawn/collect.\n   - GymWeapons: extend existing per-drop Graphics to also draw the bubble around the weapon icon, still scaled with lifecycle.\n3. Update overlap helpers to use the new size (already parametric — verify) and update any tests that hard-code 16 or near-field distances.\n4. Verify visually in both gyms and via tests.\n\n## Key Files\n\n- `src/core/constants.ts` — Double `POWER_UP_DROP_SIZE` (16 → 32); `WEAPON_DROP_SIZE` follows; single source of truth.\n- `src/powerups/icons.ts` — Add bubble-drawing helper (e.g., `drawPowerUpBubble` / `drawDropWithBubble`) or extend existing icon draw to include glowing circle; neon style, per-type colour.\n- `src/scenes/gym/GymPowerUps.ts` — Add per-drop Graphics for icon + bubble; scale with `powerUp.currentScale`; create/destroy lifecycle; overlap uses new size.\n- `src/scenes/gym/GymWeapons.ts` — Extend per-drop Graphics to include bubble alongside weapon icon; keep lifecycle scaling and spawn/despawn audio.\n- `src/powerups/PowerUp.ts` — Update constant comment/reference if needed (re-exports `POWER_UP_DROP_SIZE` mirror).\n- `src/scenes/gym/GymPowerUps.test.ts`, `src/scenes/gym/GymWeapons.test.ts`, `src/powerups/PowerUp.test.ts` — Update or add assertions for doubled size, bubble presence, and scaled overlap.\n\n## Related work\n\n- `docs/Game Design Document.md` §4.4 Power-Ups, §6.4 architecture, §7.1 neon aesthetic — authoritative spec for drop appearance.\n- Work item \"Power-up core lifecycle + tunable constants\" (AH-0MTCNZPHN003C5E1) — introduced `POWER_UP_DROP_SIZE` and lifecycle constants.\n- Work item \"Power-up catalogue + effects (P5, P8, P9)\" (AH-0MTCNZX16007ECPS) — non-combat catalogue.\n- Work item \"Create gym scene for power-ups with spawning, collection, and standalone HUD\" (AH-0MTC0QPS60005MNT) — GymPowerUps scene (currently no drop graphics).\n- Work item \"Weapon power-ups (3 patterns + reset) with auto-fire and weapon gym scene\" (AH-0MTC1TEXR006I5XR) — GymWeapons scene (has drop graphics).\n- Work item \"Create gym scene for power-ups with spawning, collection, and standalone HUD\" children: spawner, lifecycle, HUD — context for constants layout.\n\n## Risks and assumptions\n\n- **Scope-creep risk:** keep to sizing + bubble only; do not add new power-up types, particle systems, screen-shake, magnet visual changes, or HUD resizing in this item. Record extras as linked work items.\n- **Visual tuning is subjective:** bubble radius, stroke width, glow alpha, and per-type colour may need playtest iteration. Mitigation: expose bubble constants alongside `DROP_SIZE` in `src/core/constants.ts` (e.g., `POWER_UP_BUBBLE_RADIUS_FACTOR`, `BUBBLE_GLOW_ALPHA`) so feel is tunable without code churn; tests assert geometry, not exact colour.\n- **Assumption — both drop families in scope:** \"Power ups need to be larger\" is read as applying to all on-field drops (P5/P8/P9 and Spread/Dual/Rapid/Reset). If operator intended only non-combat drops, note as a follow-up — but default to all for consistency.\n- **Assumption — bubble is visual only:** the bubble does not add extra collection radius beyond the doubled drop size; overlap stays `DROP_SIZE * scale + hull`. If a larger aura-based pickup is desired, file a follow-up.\n- **Assumption — HUD unchanged:** HUD icons remain at 8 px in `src/ui/HUD.ts`; only field drops grow. No GDD §4.4 count/ID changes needed.\n- **GymPowerUps graphics gap:** adding Graphics per drop is behaviour-neutral for logic tests (harness must support `add.graphics`) but may require test updates; verify `src/test/gameHarness.ts` and GymPowerUps tests.\n\n## Appendix: clarifying questions & answers\n\n- Q1: \"Should both families of drops (non-combat P5/P8/P9 and weapon Spread/Dual/Rapid/Reset) be doubled and get the bubble, or only the non-combat drops shown in GymPowerUps?\" — Answer: **Assumed both** — brief scopes both `POWER_UP_DROP_SIZE` and `WEAPON_DROP_SIZE` (identical constant) and both gym scenes. Rationale: title says \"Power ups\" generically and constants are shared; a split would be noted as a follow-up. Source: no operator reply requested; inference from repo search (keyword `power` hits both families, constants shared at 16 px). Traceability: `src/core/constants.ts:63/98`.\n- Q2: \"Is the bubble purely visual, or should its radius also count for collection (aura pickup)?\" — Answer: **Assumed visual only** — collection stays `DROP_SIZE * scale + hull` with the doubled size; bubble does not add extra radius. Rationale: original request says \"a bubble around them (a glowing circle)\" — visual phrasing, not \"larger pickup aura\". Source: intake draft assumption; no operator reply solicited (seed context sufficient). Traceability: `src/scenes/gym/GymPowerUps.ts:_overlapsShip`, `src/scenes/gym/GymWeapons.ts:_overlapsShip`.\n- Q3: \"Any preference for bubble styling (per-type neon colour vs white glow, thickness, pulsing)?\" — Answer: **Assumed neon-style outline circle with subtle outer glow, centred at drop, radius ~1.3–1.5× drop size, scaled with lifecycle, per-type aura colour matching `ICON_COLORS` / weapon colours.** Rationale: matches GDD §7.1 neon vector aesthetic and existing `ICON_COLORS` mapping in `src/powerups/icons.ts`; keeps the implementation code-first with no assets. Source: repo inspection of `src/powerups/icons.ts` and GDD §7.1. Traceability: `src/powerups/icons.ts:ICON_COLORS`.\n\nPotentially related docs\n\n- `docs/Game Design Document.md` — §4.4 Power-Ups (drop semantics), §6.4 powerups/ layout, §7.1 neon aesthetic (bubble styling constraint).\n- `README.md` — Gym coverage / \"Run the game\" context if visuals are noted.\n- `src/test/gameHarness.ts` — harness for gym scene tests; GymPowerUps graphics addition must remain testable.\n\nPotentially related work items\n\n- AH-0MTCNZPHN003C5E1 Power-up core lifecycle + tunable constants (completed)\n- AH-0MTCNZX16007ECPS Power-up catalogue + effects (P5, P8, P9) (completed)\n- AH-0MTC0QPS60005MNT Create gym scene for power-ups with spawning, collection, and standalone HUD (completed)\n- AH-0MTC1TEXR006I5XR Weapon power-ups (3 patterns + reset) with auto-fire and weapon gym scene (completed)\n- AH-0MTCNZPHN003C5E1 / AH-0MTCPL9NA0025ZUI surrounding spawner lifecycle items (completed)\n","effort":"small","id":"AH-0MTG5MGPZ00986B4","issueType":"feature","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"low","sortIndex":100,"stage":"in_review","status":"completed","tags":[],"title":"Power ups need to be larger","updatedAt":"2026-09-09T21:54:13.379Z"},"type":"workitem"}
{"data":{"assignee":"@map","createdAt":"2026-09-02T15:40:56.900Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Thruster hum: add tonal randomization and noise/grit\n\n## Problem Statement\nFlat continuous buzz — the thruster hum reads as too clean/synthetic. Needs tonal variation (frequency drift/randomization) and noise/grit texture so it feels like a lived-in jet engine rather than a pure tone.\n\nDiscovered-from:AH-0MTFOSOHN001Q620\n\n## User Stories\n- As a player holding thrust, I hear subtle pitch drift and gritty noise so the hum doesn\\'t sound like a flat buzzer.\n- As a developer, the thruster still respects AudioContext absence (headless no-op), volume ceiling ≤0.2, and single reused gain lifecycle (no leaks).\n\n## Acceptance Criteria\n- **AC1** — Thruster hum adds small randomized/drifting detune to oscillators (e.g. detune variance or slow frequency modulation) so sustained thrust is not perfectly static.\n- **AC2** — Thruster hum adds/complicates noise/grit layer (e.g. filtered noise with randomized Q/cutoff or gain wobble) so it reads as jet-like rather than pure oscillators.\n- **AC3** — Gain envelope still respects 30 ms growth / ~4× decay, volume ≤ 0.2, clamped level [0,1], and safe no-op without AudioContext. No leaked AudioNodes on rapid toggle.\n- **AC4** — Single ship-level hum remains distinct from existing blip cues; tests updated and full suite passes.\n\n## Key Files\n- src/audio/effects.ts\n- src/audio/effects.test.ts\n- src/entities/Player.ts (wiring unchanged, verify)\n","effort":"","id":"AH-0MTK9JP37003MJQ4","issueType":"feature","needsProducerReview":false,"parentId":null,"priority":"medium","risk":"","sortIndex":300,"stage":"in_review","status":"completed","tags":[],"title":"Thruster hum: add tonal randomization and noise/grit","updatedAt":"2026-09-09T21:53:55.192Z"},"type":"workitem"}
{"data":{"assignee":"Map","createdAt":"2026-09-02T15:53:28.066Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Thruster hum: jet-engine roar — remove buzzy sawtooth\n\n## Problem Statement\nCurrent thruster hum is too buzzy (sawtooth 85 Hz + detune jitter reads as a flat buzzer). Operator wants a jet-engine roar — broadband filtered noise whoosh with soft low rumble, not a harsh sawtooth.\n\nDiscovered-from:AH-0MTK9JP37003MJQ4\n\n## User Stories\n- As a player holding thrust I hear a jet-engine roar (filtered noise whoosh + soft low rumble) rather than a buzzy sawtooth, still thrust-scaled and envelope-clean.\n- As a developer the hum still respects volume ≤0.2, 30 ms growth / ~4× decay, clamped [0,1], single reused gain, and safe no-op without AudioContext.\n\n## Acceptance Criteria\n- **AC1** — Architecture is jet-like: soft oscillator(s) (triangle/sine) for low rumble plus dominant band-pass/low-pass filtered white-noise whoosh; sawtooth harshness removed; no per-frame buzzy detune harshness.\n- **AC2** — Retains thrust-scaled gain target  (≤0.15), 30 ms growth / ~4× decay, clamped [0,1], retrigger-from-currentGain, no clicks.\n- **AC3** — Single ship-level hum, reused nodes (no leak on rapid toggle), safe no-op without AudioContext, respects autoplay.\n- **AC4** — Distinct from blip cues (sine/square/triangle blips); tests updated; GDD §7.3 Player Audio Character row updated; full suite green (31 files / 590+ tests).\n\n## Key Files\n- src/audio/effects.ts\n- src/audio/effects.test.ts\n- docs/Game Design Document.md (GDD §7.3)\n","effort":"","id":"AH-0MTK9ZSOX009SB1K","issueType":"feature","needsProducerReview":false,"parentId":null,"priority":"high","risk":"","sortIndex":100,"stage":"in_review","status":"completed","tags":[],"title":"Thruster hum: jet-engine roar — remove buzzy sawtooth","updatedAt":"2026-09-09T21:54:27.245Z"},"type":"workitem"}
{"data":{"assignee":"Map","createdAt":"2026-09-05T23:47:57.059Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# [Refactor] Deduplicate thruster teardown in effects.ts\n\n## Problem\n`src/audio/effects.ts` duplicates the thruster hum teardown sequence (`cancelScheduledValues → setValueAtTime(0) → osc.stop → sub.stop → noise.stop → thrusterHum = null`) across three call-sites with slight variations (different stop offsets, missing `noiseFilter` handling, inconsistent error wrapping). Adding a node to the thruster graph (e.g. the noise/filter layer in AH-0MTK9JP37003MJQ4) requires patching three places — easy to miss one and leave orphaned nodes or inconsistent test resets.\n\n## Users\n- **Engineer maintaining audio / thruster SFX** — can evolve the thruster node graph in one place without hunting three teardown paths.\n- **Test author / CI** — thruster lifecycle and reset semantics remain predictable and leak-free.\n\n**User stories:**\n- As an audio engineer I can add or remove a node (e.g. `noiseFilter` disconnect, new oscillator) from the thruster hum graph by editing a single helper so teardown stays consistent.\n- As a test author I can call `_resetThrusterHumForTests()` or `_resetAudioContextForTests()` and get the same safe no-op / stop semantics as before, with `AudioContext` re-seeding still working.\n\n## Acceptance Criteria\n- [ ] Teardown logic lives in a single internal helper (e.g. `teardownThrusterHum(ctxTime, stopOffset)` or similar) and the three call-sites (`_resetThrusterHumForTests`, `stopThrusterSound`, `_resetAudioContextForTests`) delegate to it — no duplicated `cancelScheduledValues`/`stop` sequences remain.\n- [ ] `stopThrusterSound` retains its 20 ms stop offset (`currentTime + 0.02`) for graceful ramp; test resets retain immediate-stop semantics where currently used; `audioCtx = null` handling stays in the `_resetAudioContextForTests` path (and `thrusterHum = null` in all paths).\n- [ ] No behaviour change: existing thruster lifecycle tests pass — leak/no-orphan guarantee, decay/ramp still correct, headless (no AudioContext) no-op still safe, and re-seeding the AudioContext via reset still works.\n- [ ] `npx tsc --noEmit` and `npm test` pass; no new lint/type errors.\n- [ ] All related documentation/comments updated (REFACTOR marker removed or updated, inline comments around the helper) and existing `src/audio/effects.test.ts` coverage still green — full project test suite must pass.\n\n## Constraints\n- Keep test-visible reset semantics unchanged — `_resetThrusterHumForTests()` and `_resetAudioContextForTests()` remain exported and must still allow worktrees to re-test the hum lifecycle in isolation and to re-seed the cached `AudioContext`.\n- Preserve `thrusterHum = null` clearing in all teardown paths and `audioCtx = null` clearing in the `_resetAudioContextForTests` path.\n- Keep error handling safe no-op (try/catch or optional chaining) — teardown must never throw, even if nodes are already stopped or `AudioContext` is absent.\n- Minimal scope — only `src/audio/effects.ts` (and tests if needed for assertions); no public API change beyond the internal helper.\n\n## Existing state\n- `src/audio/effects.ts` has three duplicated teardown blocks:\n  - `_resetThrusterHumForTests()` (lines ~108–118): `cancelScheduledValues → setValueAtTime(0) → osc.stop(ctxTime) → sub.stop(ctxTime) → noise.stop(ctxTime) → thrusterHum = null`, wrapped in try/catch.\n  - `stopThrusterSound()` (lines ~280–290): same sequence but `stop(ctxTime + 0.02)` and `thrusterHum = null`.\n  - `_resetAudioContextForTests()` (lines ~297–300): only `thrusterHum?.gain?.gain?.cancelScheduledValues?.(ctxTime)` + `thrusterHum = null; audioCtx = null` — does not stop oscillators/noise.\n- REFACTOR marker `AH-0MTP19JIB004ORC0` is present at the top of the file.\n- `ensureThrusterHum()` and `updateThrusterSound()` rely on the reset helpers for orphaned-context teardown.\n\n## Desired change\n- Extract a single internal helper such as `teardownThrusterHum(ctxTime: number, stopOffset: number)` (or `stopOffset = 0` for immediate) that performs the shared sequence: `cancelScheduledValues`, `setValueAtTime(0)`, `osc.stop`, `sub.stop`, `noise.stop` (and, if decided, `noiseFilter.disconnect`), with consistent error handling, and have the three call-sites delegate to it. Keep `thrusterHum = null` assignment and `audioCtx = null` assignment in the appropriate callers.\n- Remove the REFACTOR marker for AH-0MTP19JIB004ORC0 once deduplicated (consistent with other refactor items).\n- No change to runtime thruster hum behaviour, gain ramping, or public exports.\n\n## Key Files\n- `src/audio/effects.ts` — Primary: extract helper and update the three call-sites.\n- `src/audio/effects.test.ts` — Verify: thruster lifecycle, `stopThrusterSound` AC5, leak tests, headless no-op; may assert helper indirectly.\n- `src/entities/Player.ts` — Consumer of `stopThrusterSound`/`updateThrusterSound` (destroy/respawn paths) — no change expected, but confirms no-op safety.\n\n## Related work\n- Potentially related work items: Thruster hum: add tonal randomization and noise/grit (AH-0MTK9JP37003MJQ4) — added the `noise`/`noiseFilter` layer that made the duplication risky; Implement thruster hum in effects.ts (AH-0MTHF3SA7009A4LK); Wire thruster audio in Player.ts (AH-0MTHF3SJL009T2QL); Test: thruster hum oscillator/gain/no-op unit tests (AH-0MTHF3S0U000UZJU); [Refactor] Remove unused export THRUSTER_HUM_DETUNE_SPREAD_CENTS (AH-0MTP19DG100472CV); Player thruster SFX epic (AH-0MTFOSOHN001Q620).\n- Potentially related docs: `src/audio/effects.ts` (source of duplication), `docs/Game Design Document.md` §7.3 Player Audio Character, `docs/ENEMY_DESIGN_AND_IMPLEMENTATION.md` §7 audio notes.\n\n## Risks & Assumptions\n- **Risk:** Unifying stop offsets (0 vs 0.02) changes the audible tail or breaks the decay test — *Mitigation:* helper takes `stopOffset` param; `stopThrusterSound` passes 0.02, test resets pass 0.\n- **Risk:** `_resetAudioContextForTests` currently does not stop oscillators — unifying to a full stop could mask or alter existing test expectations — *Mitigation:* decide explicitly whether the helper's full stop is desired for the reset path and update tests accordingly; default to preserving current reset semantics unless tests show orphaned nodes.\n- **Risk:** `noiseFilter.disconnect` handling is underspecified — adding it may be unnecessary or may throw on mocks — *Mitigation:* keep disconnect optional / guarded, or leave out if not required; document decision.\n- **Assumption:** `ThrusterHumState` node set (`osc`, `sub`, `noise`, `noiseFilter`, `gain`, `ctx`) is stable and the mock AudioContext in tests supports `cancelScheduledValues`/`setValueAtTime`/`stop`.\n- **Scope-creep risk:** Do not expand to broader audio refactor (e.g. generic blip helper, gain-envelope utilities) — record any such opportunities as separate `discovered-from:AH-0MTP19JIB004ORC0` items.\n\n## Appendix: Clarifying Questions & Answers\n- No interview questions were required — the work item description already provides a clear problem statement, desired helper shape, file scope, and measurable ACs, and repo inspection (`src/audio/effects.ts` lines 108–300 and `rg` for thruster teardown call-sites) confirms the three duplicated blocks and their variations. No assumptions beyond what is documented above; any offset / `noiseFilter` handling uncertainty is captured as a Risk and will be decided during implementation with tests as the oracle.\n\n","effort":"Extra Small","id":"AH-0MTP19JIB004ORC0","issueType":"task","needsProducerReview":false,"parentId":null,"priority":"low","risk":"Low","sortIndex":100,"stage":"in_review","status":"completed","tags":[],"title":"[Refactor] Deduplicate thruster teardown in effects.ts","updatedAt":"2026-09-09T21:53:48.149Z"},"type":"workitem"}
{"data":{"assignee":"Map","createdAt":"2026-09-06T09:14:05.824Z","createdBy":"","deleteReason":"","deletedBy":"","dependencies":[],"description":"# Stale formation entities crash on GymEnemies scene restart\n\n## Problem statement (1 sentence)\n\nWhen the user navigates gym index → enemy scene → back to index → **another** enemy scene, the restarted `GymEnemies` scene still ticks the previous run's destroyed entities, which dereference their now-`undefined` `scene` property and throw `TypeError: Cannot read properties of undefined (reading 'time' / 'tweens')`.\n\n## Users\n\n- **Developer / tester**: Loads one gym scene, returns to the index, then opens a second gym scene. The second scene intermittently crashes on startup (Diver/Phaser) or after a few seconds (Tank destroyed by a player bullet).\n\n## Reproduction\n\n1. Boot `npm run dev`, land on the Gym Index.\n2. Open an enemy scene (e.g. Tank).\n3. Click \"← INDEX\" to return to the index.\n4. Open a **different** enemy scene (e.g. Swarm).\n5. Observe a crash — stack varies by stale entity type left behind:\n\n- Stale **Phaser**: `PhaserEntity.applyFormationPosition (Phaser.ts:364)` — `this.scene.time.now` on the first frame.\n- Stale **Diver**: `Diver._handleFormation (Diver.ts:433)` — `this.scene.time.now` on the first frame.\n- Stale **Tank**: `Tank.playExplosion (Tank.ts:153)` — `this.scene.tweens`, hit by a player bullet a moment after the Swarm scene opens.\n\n## Root cause (confirmed in Phaser v4.2.1 source)\n\n- `GymEnemies` is a **single registered scene instance** (`scene.add('GymEnemies', cls)`) reused across every enemy entry via `scene.start('GymEnemies', { enemyKey })` (see `GymIndex.ts`).\n- Leaving for the index (`scene.start('GymIndex')`, `gymNavigation.ts`) **stops** GymEnemies: `SceneManager.stop → Systems.shutdown` fires the SHUTDOWN event, and the scene **DisplayList.shutdown destroys every display-list child** — `list[i].destroy(true)` — which sets each entity Container's `scene = undefined` (`GameObject.destroy`, `GameObject.js:1060`).\n- `GymFormationScene` **never clears its bookkeeping arrays on shutdown**: `entities`, `bullets`, `playerBullets` (and `player`, `playerExplosions`) still reference the destroyed objects.\n- On restart, `GymEnemies.create()` runs again on the **same instance** and **pushes a fresh formation on top of the stale array** — so `tick()` now iterates destroyed objects whose `scene` is `undefined`. Any per-frame `this.scene` access throws:\n  - `Diver._handleFormation` / `PhaserEntity.applyFormationPosition` read `this.scene.time.now` → crash on the first tick.\n  - `_handleCollisions` calls `destroySelf()` on a stale Tank (still `_alive` — Phaser's destroy doesn't clear the custom flag) → `playExplosion()` reads `this.scene.tweens` → crash.\n\nThe same leak affects every `GymFormationScene` subclass (GymEnemies, GymBoss, …) on stop/restart, and any scene that repeatedly `create()`s on a reused instance.\n\n## Acceptance Criteria\n\n- [ ] **AC1 — Repeated navigation does not crash**: Playing gym index → enemy A → index → enemy B (repeat several times, covering Tank/Swarm/Diver/Phaser/Scout) never throws a `TypeError` and both formations render/behave normally.\n- [ ] **AC2 — No stale entities ticked**: After a restart, the scene's `entities`/`bullets`/`playerBullets` arrays contain exactly the freshly spawned objects (no leftover references from the previous run).\n- [ ] **AC3 — Per-frame entity code no longer depends on a live `this.scene`**: Entities ticked by the base scene use dt-driven local state (Scout-style) rather than `this.scene.time.now` where it is not required.\n- [ ] **AC4 — Regression test**: A unit test reproduces stop → restart of the same `GymFormationScene` instance and asserts no crash and no entity doubling.\n- [ ] **AC5 — Full suite green**: `npm test` passes; no change to public APIs or single-run behaviour.\n\n## Key Files\n\n- `src/scenes/gym/core/GymFormationScene.ts` — holds the leaked `entities`/`bullets`/`playerBullets` arrays; needs a SHUTDOWN teardown that clears them (destroy calls are idempotent in Phaser: `GameObject.destroy` early-returns once `scene` is undefined).\n- `src/scenes/gym/GymEnemies.ts` / `GymIndex.ts` — same scene instance reused per enemy key (restart vector).\n- `src/entities/Diver.ts` (`_handleFormation`), `src/entities/Phaser.ts` (`applyFormationPosition`, `shootEnabled`) — per-frame `this.scene.time.now` reads; convert to dt-driven local phase to remove the hard dependency.\n- `src/entities/{Tank,Scout,Swarm,Diver,Phaser}.ts` `playExplosion()` — `this.scene.tweens`; safe to keep once the scene no longer ticks destroyed entities, but a null-scene guard is a cheap belt-and-braces.\n\n## Desired change\n\nPrimary fix (root cause): in `GymFormationScene.create()`, register a SHUTDOWN listener (or extend the existing one) that tears the scene down:\n\n1. `for (const entity of this.entities) entity.destroy(true)`; clear `entities`.\n2. Destroy + clear `bullets` graphics, `playerBullets`, `playerExplosions`; `player = null`.\n3. Reset scene toggle state (`shootEnabled = false`, cancel respawn countdown).\n\nSecondary hardening (defense-in-depth): give Diver/Phaser a local phase accumulator (Scout pattern) so per-frame positioning never needs `this.scene`; add `destroy()` no-op safety where the stale-object crash manifested.\n\n## Related work\n\n- AH-0MT99G631 — Create E2 Diver gym scene\n- AH-0MTAN4PI0 — Rewrite GymDiver gym scene on the core library\n- AH-0MTG5MGPZ0 — earlier GymEnemies/config work (formation respawn on same instance)\n\n## Constraints\n\n- No public API change to entities or `GymFormationScene`.\n- Single-run behaviour must be byte-for-byte unchanged (the leak only manifests across a stop/restart cycle).\n- Keep the fix in the `core` base so GymBoss and future subclasses inherit it.\n\n## Risks & assumptions\n\n- **Assumption**: The only restart vector is the gym index re-entering the same scene instance. If other flows stop/start a gym scene, the teardown still applies (it is a SHUTDOWN hook).\n- **Risk**: Double-destroy during shutdown if both the display list and our teardown destroy the same object — Phaser's `GameObject.destroy` is idempotent (early-return when `scene` is undefined), and subclass `destroy()` overrides call child `Graphics.destroy()` first, which is likewise guarded. Verified against `GameObject.js` + `DisplayList.js`.\n- **Risk**: SHUTDOWN listeners registered per-`create()` accumulate across restarts. Keep teardown idempotent (it is) or switch to a single registration guarded by a flag.\n- **Scope**: Audio, boss-pattern, and HMR-specific behaviour are out of scope.\n\n## Appendix: clarifying questions & answers\n\n- Q: \"Which screen crashes — the Diver screen or the general Phaser screen?\" — Answer (operator): \"The problem is when I open the Phaser screen not the Diver screen.\" Then: \"If I play one gym scene then go back to the index and play another scene it sometimes fails.\" Research: three distinct stacks (Diver `time`, Phaser `time`, Tank `tweens`) all dereference `this.scene` on objects that were destroyed when the previous scene run shut down; all routed through the same reused `GymEnemies` instance (confirmed in `GymIndex.ts`, `gymNavigation.ts`, and Phaser `DisplayList.shutdown`/`GameObject.destroy`). Source: interactive replies.\n","effort":"Small","id":"AH-0MTPLHLZ3006MOC4","issueType":"bug","needsProducerReview":false,"parentId":null,"priority":"high","risk":"Low","sortIndex":100,"stage":"in_review","status":"completed","tags":[],"title":"Stale formation entities crash on GymEnemies scene restart (Tank/Swarm/Diver/Phaser)","updatedAt":"2026-09-09T21:54:21.714Z"},"type":"workitem"}
{"data":{"auditedAt":"2026-09-09T21:54:09.012Z","author":"rgardler","rawOutput":null,"readyToClose":true,"summary":"Approved by manual review","workItemId":"AH-0MTC2P6G3007PJ40"},"type":"audit_result"}
{"data":{"auditedAt":"2026-09-09T21:54:03.246Z","author":"rgardler","rawOutput":null,"readyToClose":true,"summary":"Approved by manual review","workItemId":"AH-0MTFOSOHN001Q620"},"type":"audit_result"}
{"data":{"auditedAt":"2026-09-09T21:53:40.135Z","author":"rgardler","rawOutput":null,"readyToClose":true,"summary":"Approved by manual review","workItemId":"AH-0MTFXKA5Q003LBH5"},"type":"audit_result"}
{"data":{"auditedAt":"2026-09-09T21:54:13.379Z","author":"rgardler","rawOutput":null,"readyToClose":true,"summary":"Approved by manual review","workItemId":"AH-0MTG5MGPZ00986B4"},"type":"audit_result"}
{"data":{"auditedAt":"2026-09-09T21:53:55.192Z","author":"rgardler","rawOutput":null,"readyToClose":true,"summary":"Approved by manual review","workItemId":"AH-0MTK9JP37003MJQ4"},"type":"audit_result"}
{"data":{"auditedAt":"2026-09-09T21:54:27.245Z","author":"rgardler","rawOutput":null,"readyToClose":true,"summary":"Approved by manual review","workItemId":"AH-0MTK9ZSOX009SB1K"},"type":"audit_result"}
{"data":{"auditedAt":"2026-09-09T21:53:48.149Z","author":"rgardler","rawOutput":null,"readyToClose":true,"summary":"Approved by manual review","workItemId":"AH-0MTP19JIB004ORC0"},"type":"audit_result"}
{"data":{"auditedAt":"2026-09-09T21:54:21.714Z","author":"rgardler","rawOutput":null,"readyToClose":true,"summary":"Approved by manual review","workItemId":"AH-0MTPLHLZ3006MOC4"},"type":"audit_result"}
